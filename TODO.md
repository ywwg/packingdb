# TODO

* easy first thing: Get rid of global registration crap -- that should all be in a registry object.
  * registry should be an interface, that will work well with API in the future.
* Start on V3 to separate contexts and properties -- I still think it's ok for a context to have an automatic associated property for requirement checking though.
* Tests for contexts!!
* Rip styling out of trip (comment for now)
* rewrite menuing
* abandon CLI
