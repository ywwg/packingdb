* add the rest of the features like setting temperature, etc
* complete my stateful list change, try to upstream it
 * (scroll position changes unnecessarily so we need to persist it)
 * or include in my own tree
* try to split checkboxes out from Strings()
* pgup pgdn -- even possible?
